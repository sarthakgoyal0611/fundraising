<body style="background-color:oldlace;">
<h1 class="text-center mt-4"> <%= @user.first_name%>'s profile </h1>

<h3>
    <i> Full Name- <%= @user.first_name + ' '+ @user.last_name %>
<br>
    <i> Email- <%= @user.email%></i>
<br>

    <i> Date of Birth- <%= @user.dob%></i>
<br>
</h3>
    <img src=<%= @user.avatar%> width="300 height="100 >
<br> 
    <%= link_to 'Edit', edit_user_registration_path %> |

    <%= link_to 'Delete Permanent', user_registration_path, method: :delete, data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?", turbo_method: :delete} %> |

    <%= link_to 'Sign-Out', destroy_user_session_path, method: :delete, data: { confirm: "Are you sure?", turbo_confirm: "Are you sure?", turbo_method: :delete} %>  |

    <%= link_to 'Home Page', root_path %>
